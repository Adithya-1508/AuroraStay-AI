# Knowledge Versioning Manual

The `KnowledgeVersioningManager` tracks document versions, embedding pipeline configurations, and vector indices.

## Purpose

Enables verification checks to prevent stale indexes and mismatched query vectors during pipeline re-runs or model upgrades.
